{
  "name": "thermal-docs",
  "version": "0.1.0",
  "description": "Documentation site for Thermal Themes — docs.thermalthemes.com.",
  "type": "module",
  "private": true,
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
